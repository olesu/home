{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CDK diff",
            "type": "shell",
            "options": {
                "env": {
                    "PATH": "/usr/local/opt/node@16/bin:${env:PATH}"
                }
            },
            "command": "npx cdk diff",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}